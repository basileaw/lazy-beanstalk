[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
serve = "python server.py"
serve-container = "bash -c 'docker build -t quote-game . && docker run -p 5000:5000 -p 7681:7681 quote-game'"

[packages]
rich = ">=13.9.4"
bs4 = ">=0.0.2"
requests = ">=2.32.3"
fastapi = "*"
uvicorn = "*"

[requires]
# System requirements:
# - Docker
# - AWS CLI
# - AWS Lightsail Control Plugin (lightsailctl)
python_version = "3.12"
system_packages = ["ttyd"]

[dev-packages]
